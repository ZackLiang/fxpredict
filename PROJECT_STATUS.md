# FX-Predict 项目状态大屏

## 1. 核心消融定义 (Ablation Dictionary)

> 严格单一变量控制：每步仅改变一个创新点，保证消融链的学术可信度。

| 版本 | 骨干 | 双图+MoE | DirLoss/DiffIC | PhysLoss | RevIN | 学术意义 |
|------|------|----------|---------------|----------|-------|---------|
| **TCN-baseline** | TCN | ❌ | ❌ | ❌ | ✅ | 原始图网络底线 |
| **M0-clean** | Mamba | ❌ | ❌ | ❌ | ✅ | 证明 Mamba vs TCN 的纯骨干差异 |
| **M1-clean** | Mamba | ✅ (双图+CrossAttn+Router) | ❌ | ❌ | ✅ | 证明双图+MoE架构升级有效 |
| **M2-clean** | Mamba | ✅ | ✅ | ❌ | ✅ | 证明方向性 Loss 引导的净增益 |
| **M3-Proposed** | Mamba | ✅ | ✅ | ✅ (0.025) | ✅ | 完全体，三角套利物理约束收尾 |

---

## 2. 当前最优超参数 (Hyper-Parameters)

> ⚠️ **注意**：所有创新点 Loss 在训练时乘以了 `batch_elements = output.size(0) * output.size(1)` 进行尺度对齐（Fix2），以下权重均为对齐后的校准值。

| 参数 | 值 | 等效量级（×batch_elements≈3968） | 占 base_loss(≈400) 比例 |
|------|----|--------------------------------|------------------------|
| `dir_weight` | **0.04** | ≈ 159 | 40% |
| `diffic_weight` | **0.02** | ≈ 79 | 20% |
| `phys_weight` | **0.025** | ≈ 99 | 25% |
| `ckpt_ic_weight` | **0.1** | — | IC 引导 checkpoint 选择 |
| `batch_size` | **128** | — | 增加梯度更新步数，有助于弱信号 Loss 学习 |
| `normalize` | **2** | — | 按最大值归一化 |
| `epochs` | **50** | — | 正式实验 |
| `runs` | **3** | — | 3 随机种子取均值，消除种子依赖 |

---

## 3. 工程 Bug 修复日志 (Changelog)

| 日期 | 修复项 | 影响 |
|------|--------|------|
| 2026-03-15 | **Fix1**: `num_split==1` 时禁用节点随机打乱 (`np.arange`) | 保护格兰杰图和 RevIN 节点索引对齐 |
| 2026-03-15 | **Fix2**: 创新点 Loss 乘以 `batch_elements` 进行尺度对齐 | DirLoss/DiffIC/PhysLoss 不再被 base_loss 淹没 |
| 2026-03-15 | `ckpt_ic_weight` 默认值 0.0→0.1 | 启用 IC 引导的 checkpoint 选择策略 |
| 2026-03-15 | `train_baselines.py` IC 计算加 `unbiased=False` | 保证 baseline 与主模型 IC 口径一致 |
| 2026-03-15 | `gen_corr_matrix.py` `+1e-8` → `.clip(lower=1e-8)` | 格兰杰图生成更稳健 |
| 2026-03-15 | `layer.py` 冗余 `.to(device)` 清理 | 代码洁癖修复 |
| 2026-03-15 | `train_baselines.py` `--normalize` 默认值 0→2 | 与主模型归一化策略对齐 |
| 2026-03-16 | **Fix3**: DA_spread 体制判断逻辑修正（详见第4节） | DA_spread 从恒负修复为有意义的正负分布 |
| 2026-03-16 | **Fix4**: 消融矩阵重构，严格单一变量控制（详见第4节） | 消融链学术可信度大幅提升 |
| 2026-03-16 | `run_final.sh` 加入 `OMP/MKL_NUM_THREADS=8` 并行安全锁 | 防止多进程抢占 21核 CPU 导致系统崩溃 |
| 2026-03-16 | `run_final.sh` `PY` 变量加入 `env PYTHONUNBUFFERED=1` | 修复 `conda run` 嵌套场景下日志不实时刷入文件的问题 |

---

## 4. 第二轮实验改进详记（2026-03-16）

> 本节记录从"第一轮失败实验"到"第二轮修正实验"的完整分析链条，防止重复踩坑。

### 4.1 第一轮实验结果（问题数据，已废弃）

**运行配置**：batch_size=256，并行8进程，消融矩阵存在控制变量污染。

| 模型 | bs | mamba | router | dir | phys | MAE | DA | IC | ICIR | CCC | DA_trend | DA_range | DA_spread |
|------|----|-------|--------|-----|------|-----|----|----|------|-----|----------|----------|-----------|
| TCN-baseline | 256 | 0 | 0 | 0 | 0 | 0.0895 | 0.5061 | 0.0064 | 0.147 | — | — | — | — |
| M0（旧，污染） | 256 | 0 | 0 | **1** | 0 | 0.0908 | 0.5098 | 0.0213 | 0.443 | 0.499 | 0.5085 | 0.5133 | **-0.0048** |
| M1（旧，污染） | 256 | 1 | 0 | **1** | 0 | 0.0901 | 0.5048 | 0.0052 | 0.121 | 0.494 | 0.5021 | 0.5120 | **-0.0099** |
| M2 | 256 | 1 | 1 | 1 | 0 | 0.0902 | 0.5076 | 0.0113 | 0.279 | 0.500 | 0.5061 | 0.5119 | **-0.0058** |
| M3 | 256 | 1 | 1 | 1 | 0.025 | 0.0897 | 0.5074 | 0.0114 | 0.302 | 0.500 | 0.5054 | 0.5129 | **-0.0075** |
| LSTM | 256 | 0 | 0 | 0 | 0 | 0.1215 | 0.5056 | 0.0175 | 0.447 | — | — | — | — |
| AGCRN | 256 | 0 | 0 | 0 | 0 | 0.1111 | 0.5043 | 0.0161 | 0.397 | — | — | — | — |
| iTransformer | 256 | 0 | 0 | 0 | 0 | 0.1002 | 0.5082 | 0.0085 | 0.194 | — | — | — | — |

### 4.2 问题诊断（三大根因）

#### 根因1：消融矩阵控制变量污染（最严重）

**原象**：M0（旧）和 M1（旧）都开着 DirLoss（`use_dirloss=1`），这导致：
- M0 vs TCN 的比较，不是"Mamba vs TCN"，而是"Mamba+DirLoss vs 纯TCN"
- M1 vs M0 的比较，变成了"有双图 vs 无双图"，但两者都带DirLoss
- **审稿人会直接以"控制变量不严格"为由拒稿**

**修复（Fix4）**：重构消融矩阵为严格单一变量链：
```
TCN-baseline（无任何创新）
  → M0-clean（仅+Mamba骨干，无Loss创新）
  → M1-clean（M0+双图+MoE，仍无Loss）
  → M2-clean（M1+DirLoss+DiffIC，无PhysLoss）
  → M3-Proposed（M2+PhysLoss=0.025，完全体）
```

#### 根因2：DA_spread 恒为负，体制判断逻辑错误

**原象**：所有模型的 DA_spread = DA_trend - DA_range < 0，即震荡行情 DA 反而高于趋势行情。

**根本原因**：原代码用"截面标准差"作为体制代理变量，这在金融上是完全错误的：
- 截面std大 → 各货币方向分化 → 这恰恰是**震荡/套利行情**（各自走走自己的）
- 真实的趋势市（如美元加息周期）= 所有非美货币**同向走弱** → 截面std反而**偏小**
- 结果：趋势市和震荡市的标签被完全搞反了

**修复（Fix3）**：改用"z-score标准化后的截面均值绝对值"：
```python
# 旧（错误）：
_cross_vol = np.abs(_all_dt).std(axis=1)   # 截面std：分化强度≠趋势强度

# 新（正确）：
_dt_std   = _all_dt.std(axis=0, keepdims=True) + 1e-8  # 各货币对时序std（去量纲）
_dt_norm  = _all_dt / _dt_std                           # z-score归一化
_cross_momentum = np.abs(_dt_norm.mean(axis=1))         # 截面均值绝对值=同向漂移强度
```
关键改进点：先做 z-score 去量纲（消除USDJPY波动0.5 vs EURUSD波动0.001的量级差异），再用截面均值绝对值衡量"市场整体同向性"。

#### 根因3：batch_size=256 梯度方差过小，弱信号 Loss 学不到

**原象**：M2（有DirLoss）vs M1（无DirLoss），IC 从 0.0052 升到 0.0113，但 DA 几乎没变（0.5048→0.5076）。

**原因分析**：batch_size=256 时，每个 batch 包含 256 个时间步，梯度估计方差很小。DirLoss 作为一个弱信号（方向对/错的 BCE），在大批次下梯度被平均，学习信号被淹没。小批次（128）虽然梯度有噪声，但增加了每 epoch 的梯度更新步数（256/128=2倍），弱信号 Loss 有更多机会被学到。

**修复**：`batch_size=256 → 128`，与控制变量统一对齐。

### 4.3 第二轮实验配置（当前正在运行）

**运行时间**：2026-03-16 00:26 启动，预计 04:30~07:00 完成

**硬件状态**：GPU 67.6GB/139GB，利用率100%，全部8进程正常运行

| 模型 | bs | mamba | router | dir | phys | 说明 |
|------|----|-------|--------|-----|------|------|
| M0-clean | **128** | 1 | **0** | **0** | 0 | 修正：去掉DirLoss污染 |
| M1-clean | **128** | 1 | **1** | **0** | 0 | 修正：去掉DirLoss污染 |
| M2-clean | **128** | 1 | 1 | 1 | 0 | 同M2，bs统一为128 |
| M3-Proposed | **128** | 1 | 1 | 1 | 0.025 | 同M3，bs统一为128 |
| TCN/LSTM/AGCRN/iTrans | **128** | 0 | 0 | 0 | 0 | bs统一为128 |

### 4.4 预期改善方向

1. **DA_spread**：修复体制判断后，预计从恒负变为 M3 > M2 > M1 的单调递进，体现Router的体制感知价值
2. **消融链单调性**：修复控制变量污染后，MAE/IC应沿 TCN→M0→M1→M2→M3 单调改善
3. **M0 vs TCN**：M0-clean（纯Mamba，无Loss加持）vs TCN-baseline，将给出真实的"Mamba对外汇预测是否有帮助"的答案

---

## 5. 实验结果追踪 (Latest Results)

### 第一轮（已废弃，消融矩阵污染 + batch=256）

| 模型 | MAE | DA | IC | ICIR | CCC | DA_spread | 问题 |
|------|-----|----|----|------|-----|-----------|------|
| TCN-baseline | 0.0895 | 0.5061 | 0.0064 | 0.147 | — | — | 参考基线 |
| M0（旧污染） | 0.0908 | 0.5098 | 0.0213 | 0.443 | 0.499 | **-0.005** | ❌ 开着DirLoss |
| M1（旧污染） | 0.0901 | 0.5048 | 0.0052 | 0.121 | 0.494 | **-0.010** | ❌ 开着DirLoss |
| M2 | 0.0902 | 0.5076 | 0.0113 | 0.279 | 0.500 | **-0.006** | ⚠️ bs=256 |
| M3 | 0.0897 | 0.5074 | 0.0114 | 0.302 | 0.500 | **-0.008** | ⚠️ bs=256 |

### 第二轮（当前进行中，2026-03-16）

> 结果待填入，预计完成后自动写入 EXPERIMENT_LOG.md

| 模型 | MAE | DA | IC | ICIR | CCC | DA_spread | 状态 |
|------|-----|----|----|------|-----|-----------|------|
| TCN-baseline | — | — | — | — | — | — | 🔄 运行中 |
| M0-clean | — | — | — | — | — | — | 🔄 运行中 |
| M1-clean | — | — | — | — | — | — | 🔄 运行中 |
| M2-clean | — | — | — | — | — | — | 🔄 运行中 |
| M3-Proposed | — | — | — | — | — | — | 🔄 运行中 |

---

## 6. 下一步行动 (Next Actions)

- [x] 修复消融矩阵控制变量污染（Fix4）
- [x] 修复 DA_spread 体制判断逻辑（Fix3）
- [x] 统一 batch_size=128
- [x] 加入并行安全锁和日志缓冲修复
- [ ] 等待第二轮实验完成，读取结果填入论文表格
- [ ] 验证 DA_spread 是否变为正值（Router有效性最直接证据）
- [ ] 验证消融链单调性（MAE: TCN→M0→M1→M2→M3 是否递减）
