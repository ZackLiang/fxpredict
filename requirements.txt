# ============================================================
# Regime-MoE-GNN 依赖包列表
# 目标环境：Python 3.8+，Linux/macOS，支持 CUDA 11.x / 12.x
# ============================================================

# ── 核心深度学习框架 ─────────────────────────────────────────
# PyTorch >= 1.12 必须（TransformerEncoderLayer batch_first=True 需要 >= 1.9，
#                       torch.quantile 需要 >= 1.7）
# CUDA 11.8 用户：pip install torch --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1 用户：pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU/MPS 用户：  pip install torch  （自动选最新稳定版）
torch>=1.12.0

# ── 数值计算 ─────────────────────────────────────────────────
numpy>=1.21.0        # np.quantile 在 1.15+ 可用；1.21 稳定支持 Python 3.8
scipy>=1.7.0         # statsmodels 的格兰杰检验依赖
pandas>=1.3.0        # gen_corr_matrix.py 数据读取

# ── 可视化 ────────────────────────────────────────────────────
matplotlib>=3.5.0    # plot_results.py 全部图表；3.5+ 支持 polar label 改进

# ── 统计检验 ─────────────────────────────────────────────────
statsmodels>=0.13.0  # gen_corr_matrix.py 格兰杰因果检验

# ── 可选（加速 & 工具）───────────────────────────────────────
# scikit-learn>=1.0.0  # 未在当前代码中使用，如需 PCA 等可安装
# mamba-ssm  # 可选：CUDA 下可安装以使用官方 Mamba 加速（需 Linux+CUDA 11.6+）
